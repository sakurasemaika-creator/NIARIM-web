# 引き継ぎの入口（AIモデル・新しい担当者向け）

このリポジトリを初めて触る人／モデルが、**最初に読む1枚**です。
ここだけ読めば作業を始められ、深い話は各文書へ渡します。

---

## 1. 60秒で分かるこのリポジトリ

手描きアニメーション制作アプリ **NIARIM** の公式サイトです。

- **Cloudflare Workers Static Assets**。フロントは素の HTML / CSS / JavaScript。
  ビルドステップは無く、`public/` の中身がそのまま配信される
- Worker（`src/index.js`）が処理するのは**お問い合わせフォームの送信だけ**
- **7言語**（ja / en / zh-Hans / zh-Hant / ko / fr / es）。URLは全言語共通で、
  `public/js/i18n.js` が `data-i18n` 属性のテキストを差し替える
- 実際にデプロイされるブランチは **`dev_branch`**（`main` ではない）
- **複数のセッションが同時に同じブランチを触っている**。push 前に必ず
  `git fetch origin dev_branch && git rebase origin/dev_branch`

このサイトの一番の特徴は、**アプリの画面を画像ではなく DOM と CSS で
再現した「画面再現図」**を大量に載せていることです。スクリーンショットを
貼っていないので、翻訳するとテキストも差し替わり、テーマ色も切り替わります。
その代わり、**アプリ本体の実装と食い違うと嘘の説明になる**ため、
再現図に手を入れるときは必ずアプリのソースを根拠にします。

---

## 2. 最初にやること

```bash
npm ci
npm run verify:env          # 足りないものと直し方だけが出る
```

`NG` が出たら表示された `→` のコマンドを実行して、もう一度。
すべて `OK`（またはアプリソースの WARN のみ）になってから先へ進みます。

配信は次のどちらでも構いません。`verify:env` と `tools/shot.mjs` は
両方を自動で探します。

```bash
npx wrangler dev --port 8788                      # 本番と同じ（推奨）
python3 -m http.server 8787 --directory public    # wrangler が使えない環境
```

---

## 3. 文書の地図（この順で読む）

| 文書                                             | 何が書いてあるか                                   | いつ読むか                 |
| ------------------------------------------------ | -------------------------------------------------- | -------------------------- |
| **この ONBOARDING.md**                           | 全体像・最初の1歩・禁止事項                        | 最初                       |
| [`HANDOFF.md`](./HANDOFF.md)                     | 経緯・ハマりどころ・残タスク・**絶対に守る方針**   | 最初（2節は必読）          |
| [`AI-VERIFICATION.md`](./AI-VERIFICATION.md)     | 見た目を自分で撮って確かめる手順                   | 見た目を触る前             |
| [`README.md`](./README.md)                       | ディレクトリ構成・デプロイ・フォーム・セキュリティ | 実装の詳細が要るとき       |
| [`DESIGN.md`](./DESIGN.md)                       | デザインの意図・タイポ・余白・モーションの規範     | 見た目を足すとき           |
| [`docs/CSS-LAYERS.md`](./docs/CSS-LAYERS.md)     | **CSSの読み込み順と詳細度**。事故が一番多い場所    | CSSを1行でも足す前         |
| [`docs/SCREEN-MOCKS.md`](./docs/SCREEN-MOCKS.md) | 画面再現図の仕組みと足し方                         | 図を触るとき               |
| [`docs/I18N.md`](./docs/I18N.md)                 | 7言語の文言の足し方                                | 文字を足すとき             |
| [`docs/FONTS.md`](./docs/FONTS.md)               | フォントのサブセット化の仕組み                     | 新しい文字・記号を使うとき |
| [`docs/AUDITS.md`](./docs/AUDITS.md)             | 監査スクリプト23本が何を守っているか               | 監査が落ちたとき           |

---

## 4. 絶対に守ること（破ると作り直しになる）

詳細と背景は `HANDOFF.md` 2節にあります。ここは要約です。

1. **アプリに無い機能・意匠を書かない。** 内容はすべて
   `sakurasemaika-creator/NIARIM`（Flutter）の実装が根拠。憶測で足さない
2. **絵文字を使わない。** アイコンは `public/assets/icons/ui/sprite.svg`。
   アプリが使っている `Icons.*` と同じ意匠を足し、`LICENSE.md` に出典を書く。
   文字記号（`★` `✓` `⋮` など）も字形が閲覧環境で変わるので避ける
3. **文言は自分で訳さない。** `lib/l10n/app_*.arb` から7言語ぶん取る
4. **価格・キャンペーンの文言を勝手に変えない。** 変更が要るときは
   お知らせ記事を足す形で相談する
5. **プリセットの「個数」を書かない**（増減するため）
6. **UI部品の専門用語をそのまま利用者向けの文章に出さない**
7. **対応デバイスの記述**（板タブ／液タブ／DeX／Sペン）は現状維持
8. **ホバー演出は実際に押せる要素だけ**に付ける
9. **`npm run format:check` を必ず通す。** CIの門番であり、他セッションと
   同時に触るため差分を最小に保つ意味もある

---

## 5. 変更の型（どれに当てはまるかで進め方が違う）

### A. 文言だけ変える

`docs/I18N.md` → 7言語ぶん足す → `npm run audit` → push。
新しい文字・記号を使った場合だけ `docs/FONTS.md` も見ること。

### B. 見た目（CSS）を変える

`docs/CSS-LAYERS.md` で**どのファイルに書くべきか**を決めてから書く。
ここを外すと「!important を足したのに効かない」に必ずなります。
その後 `AI-VERIFICATION.md` の手順で撮って目視。

### C. 画面再現図を変える

`docs/SCREEN-MOCKS.md`。**先にアプリのソースで正解を確認**してから
`public/js/main.js` と HTML のフォールバックの**両方**を直します。

### D. アプリ側の新機能をサイトに反映する

`HANDOFF.md` 6.1節に突き合わせの手順と、これまで確認済みの範囲があります。

---

## 6. 終わり方（毎回これ）

```bash
npm run format:check
BASE_URL=http://localhost:8788 npm run audit
git fetch origin dev_branch && git rebase origin/dev_branch
git push -u origin dev_branch
```

コミットメッセージは**日本語で、何をしたかではなく「なぜそうしたか」**を
書く慣習です（既存のログが参考になります）。同じ罠を後から踏まないための
記録として書いてください。

---

## 7. 詰まったときに真っ先に疑うこと

| 症状                              | まず疑う                                                     |
| --------------------------------- | ------------------------------------------------------------ |
| 修正が反映されない                | 配信サーバーが落ちている（`npm run verify:env`）             |
| `!important` を足したのに効かない | `docs/CSS-LAYERS.md`。読み込み順か詳細度                     |
| 撮ったPNGが空白・途中             | `scrollIntoViewIfNeeded` していない。`tools/shot.mjs` を使う |
| 監査が大量に落ちる                | 監査側のバグを疑う。`docs/AUDITS.md` に実際の誤検出例あり    |
| 図の寸法が合わない                | `--fd-fit` の縮小。実寸はこの値で割る                        |
| 文字だけ書体が違う                | サブセット漏れか字形の問題。`docs/FONTS.md`                  |
| push が衝突する                   | 他セッションが同じブランチにいる。rebase する                |
